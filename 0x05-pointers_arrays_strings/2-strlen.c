#include "main.h"
/**
 * _strlen - returns length of a string
 * @s: string variable
 * Return: 0 Always
 */
int _strlen(char *s)
{
	int len;

	len = _strlen(s);
	_putchar("%d\n", len);
	return (0);
}
