#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/**
 * Writes a function like macro ABS(X)
 * that computes the absolute value of number x
 */

#define ABS(x) (((x) < 0) ? -(x) : (x))

#endif
