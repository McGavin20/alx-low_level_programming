#include "main.h"
/**
 * _strcat - concatenates two strings
 * @dest: destination
 * @src: source
 * Retur dest of string
 */
char *_strcat(char *dest, char *src)
{
	char index[];

	if (index != '\0')
	{
		index++;
	}

	_strcat(dest, src);

	return (dest);
}
