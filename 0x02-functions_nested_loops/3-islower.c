#include "main.h"
/**
 * _islower - checks if a character is lowercase
 * @c: character checker
 *
 * RETURN: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
